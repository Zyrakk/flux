{{- if and .Values.secrets.create (not .Values.secrets.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "flux.secretName" . }}
  labels:
    {{- include "flux.labels" . | nindent 4 }}
type: Opaque
data:
  {{- if .Values.auth.token }}
  AUTH_TOKEN: {{ .Values.auth.token | b64enc | quote }}
  {{- end }}
  # LLM API key — set via: helm install --set llm.apiKey=<key>
  # or create the secret manually before install
  {{- if .Values.llm.apiKey }}
  LLM_API_KEY: {{ .Values.llm.apiKey | b64enc | quote }}
  {{- end }}
  {{- if .Values.reddit }}
  {{- if .Values.reddit.clientId }}
  REDDIT_CLIENT_ID: {{ .Values.reddit.clientId | b64enc | quote }}
  {{- end }}
  {{- if .Values.reddit.clientSecret }}
  REDDIT_CLIENT_SECRET: {{ .Values.reddit.clientSecret | b64enc | quote }}
  {{- end }}
  {{- if .Values.reddit.username }}
  REDDIT_USERNAME: {{ .Values.reddit.username | b64enc | quote }}
  {{- end }}
  {{- if .Values.reddit.password }}
  REDDIT_PASSWORD: {{ .Values.reddit.password | b64enc | quote }}
  {{- end }}
  {{- end }}
  {{- if .Values.github }}
  {{- if .Values.github.token }}
  GITHUB_TOKEN: {{ .Values.github.token | b64enc | quote }}
  {{- end }}
  {{- end }}
{{- end }}
