services:
  # ============================================================================
  # Infrastructure
  # ============================================================================
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-flux}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-flux}
      POSTGRES_DB: ${POSTGRES_DB:-flux}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-flux}"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - flux

  nats:
    image: nats:2-alpine
    command: ["--jetstream", "--store_dir=/data", "-m", "8222"]
    volumes:
      - nats_data:/data
    ports:
      - "4222:4222"
      - "8222:8222"  # Monitoring
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8222/healthz"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s
    networks:
      - flux

  redis:
    image: valkey/valkey:8-alpine
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - flux

  # ============================================================================
  # Application
  # ============================================================================
  embeddings-svc:
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.embeddings-svc
    environment:
      EMBEDDINGS_MODEL_PATH: /models/all-MiniLM-L6-v2
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health').read()"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s
    cpus: 0.5
    mem_limit: 512m
    networks:
      - flux

  api:
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.api
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-flux}:${POSTGRES_PASSWORD:-flux}@postgres:5432/${POSTGRES_DB:-flux}?sslmode=disable
      NATS_URL: nats://nats:4222
      REDIS_URL: redis://redis:6379/0
      LLM_PROVIDER: ${LLM_PROVIDER:-glm}
      LLM_ENDPOINT: ${LLM_ENDPOINT:-https://open.bigmodel.cn/api/coding/paas/v4}
      LLM_MODEL: ${LLM_MODEL:-glm-4.7}
      LLM_API_KEY: ${LLM_API_KEY:-}
      EMBEDDINGS_URL: http://embeddings-svc:8000
      API_PORT: "8080"
      LOG_LEVEL: ${LOG_LEVEL:-info}
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      redis:
        condition: service_healthy
      embeddings-svc:
        condition: service_healthy
    networks:
      - flux

  worker-rss:
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.worker-rss
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-flux}:${POSTGRES_PASSWORD:-flux}@postgres:5432/${POSTGRES_DB:-flux}?sslmode=disable
      NATS_URL: nats://nats:4222
      REDIS_URL: redis://redis:6379/0
      LOG_LEVEL: ${LOG_LEVEL:-info}
      WORKER_MODE: ${WORKER_MODE_RSS:-daemon}
      RATE_LIMITS: ${RATE_LIMITS:-reddit.com=60/min,hacker-news.firebaseio.com=30/min,api.github.com=83/min,default=10/min}
      USER_AGENT: ${USER_AGENT:-Flux/1.0 (+https://github.com/zyrak/flux)}
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - flux

  worker-hn:
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.worker-hn
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-flux}:${POSTGRES_PASSWORD:-flux}@postgres:5432/${POSTGRES_DB:-flux}?sslmode=disable
      NATS_URL: nats://nats:4222
      REDIS_URL: redis://redis:6379/0
      LOG_LEVEL: ${LOG_LEVEL:-info}
      WORKER_MODE: ${WORKER_MODE_HN:-daemon}
      HN_MIN_SCORE: ${HN_MIN_SCORE:-10}
      RATE_LIMITS: ${RATE_LIMITS:-reddit.com=60/min,hacker-news.firebaseio.com=30/min,api.github.com=83/min,default=10/min}
      USER_AGENT: ${USER_AGENT:-Flux/1.0 (+https://github.com/zyrak/flux)}
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - flux

  processor:
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.processor
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-flux}:${POSTGRES_PASSWORD:-flux}@postgres:5432/${POSTGRES_DB:-flux}?sslmode=disable
      NATS_URL: nats://nats:4222
      REDIS_URL: redis://redis:6379/0
      EMBEDDINGS_URL: http://embeddings-svc:8000
      RELEVANCE_THRESHOLD_DEFAULT: ${RELEVANCE_THRESHOLD_DEFAULT:-0.30}
      RELEVANCE_THRESHOLD_MIN: ${RELEVANCE_THRESHOLD_MIN:-0.15}
      RELEVANCE_THRESHOLD_MAX: ${RELEVANCE_THRESHOLD_MAX:-0.60}
      RELEVANCE_THRESHOLD_STEP: ${RELEVANCE_THRESHOLD_STEP:-0.05}
      SOURCE_BOOSTS: ${SOURCE_BOOSTS:-tl;dr sec=0.1}
      LOG_LEVEL: ${LOG_LEVEL:-info}
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      embeddings-svc:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - flux

  briefing-gen:
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.briefing-gen
    profiles: ["manual"]
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-flux}:${POSTGRES_PASSWORD:-flux}@postgres:5432/${POSTGRES_DB:-flux}?sslmode=disable
      NATS_URL: nats://nats:4222
      REDIS_URL: redis://redis:6379/0
      LLM_PROVIDER: ${LLM_PROVIDER:-glm}
      LLM_ENDPOINT: ${LLM_ENDPOINT:-https://open.bigmodel.cn/api/coding/paas/v4}
      LLM_MODEL: ${LLM_MODEL:-glm-4.7}
      LLM_API_KEY: ${LLM_API_KEY:-}
      BRIEFING_MODE: cronjob
      BRIEFING_SCHEDULE: ${BRIEFING_SCHEDULE:-0 3 * * *}
      RELEVANCE_THRESHOLD_DEFAULT: ${RELEVANCE_THRESHOLD_DEFAULT:-0.30}
      RELEVANCE_THRESHOLD_MIN: ${RELEVANCE_THRESHOLD_MIN:-0.15}
      RELEVANCE_THRESHOLD_MAX: ${RELEVANCE_THRESHOLD_MAX:-0.60}
      LOG_LEVEL: ${LOG_LEVEL:-info}
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
    networks:
      - flux

volumes:
  postgres_data:
  nats_data:
  redis_data:

networks:
  flux:
    driver: bridge
